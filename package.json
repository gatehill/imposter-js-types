{
  "name": "imposter-types",
  "version": "1.0.0",
  "description": "",
  "types": "./index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc && cp package.json ./dist",
    "clean": "rimraf ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  }
}
